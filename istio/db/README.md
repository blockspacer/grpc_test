# About

```bash
sudo systemctl daemon-reload
sudo systemctl restart docker

docker pull postgres:9.6.17 --disable-content-trust

docker pull mongo:4.2.3-bionic --disable-content-trust

docker pull mysql:5.6 --disable-content-trust

docker pull gcr.io/google_samples/gb-redisslave:v3 --disable-content-trust
```
